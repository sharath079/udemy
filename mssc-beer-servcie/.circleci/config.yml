version: 2.1
orbs:
    maven: circleci/maven@1.1
jobs:
    build:
        docker:
          - image: circleci/openjdk:8-browsers-legacy
        steps:
          - checkout
          - run: mvn package
